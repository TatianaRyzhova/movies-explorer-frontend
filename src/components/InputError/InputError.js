import React from "react";

function InputError(props) {
  return (
    <span id={`${props.name}-input-error`} className="input-error">
      {/*Test error message something is wrong*/}
    </span>
  )
}

export default InputError;
